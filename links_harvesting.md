### lynx (not only a textmode browser) ###
`$ lynx -dump <url>`

### lwp-request (libwww-perl) ###
`$ lwp-request|GET <url> -o links`

Python can do this, but shell + tools + gparallel is faster and easier to maintain.